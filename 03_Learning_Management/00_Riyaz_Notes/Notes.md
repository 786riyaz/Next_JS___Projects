I have given you complete code of my next js application.
the applicaton is working some what perfect but still have some issues.
so i want to fix it with some UI changes.

and i want some modification on priorirty page. where i want up and down button in each row using which i can change the learning order or module order of each topics and the UI should update according to it.
And this feature should also work properly with domain filterd applied condition. and yes these button should be hidden if any ordering is applied as it can confuse the user.

and on links page i want sorting options on category, topics and subtopics columns.

all the other things are working just fine so dont touch any thing. do not break any existing feature, functionality and any other UI.

and let me know if you need any thing from my side.
and give the response in such a way to consume less tokens with concise code, this way we can talk more and more. 
and give me complete fixed code files instead of patches.